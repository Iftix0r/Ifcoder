# Nusxa oling: cp .env.example .env
# .env fayl .gitignore'da — hech qachon commit qilinmaydi.

# Bo'sh qoldirsangiz, config/settings.py'dagi lokal fallback qiymatlar ishlatiladi.
# Production'da (cPanel) bularni Setup Python App > Environment Variables orqali bering.

DJANGO_SECRET_KEY=
DJANGO_DEBUG=True
IFCODER_FERNET_KEY=
